spring:
  application:
    name: settle-core-scheduler
  main:
    web-application-type: none
  datasource:
    url: ${SETTLE_DB_URL:jdbc:postgresql://localhost:5432/settle_core}
    username: ${SETTLE_DB_USERNAME:settle}
    password: ${SETTLE_DB_PASSWORD:settle}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
        format_sql: true

settle:
  scheduler:
    enabled: ${SETTLE_SCHEDULER_ENABLED:true}
